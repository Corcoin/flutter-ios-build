name: Build iOS IPA (no signing)

on:
  push:
    branches:
      - main

jobs:
  build_ios:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: List repo contents for debugging
        run: ls -R

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.1'

      - name: Install dependencies
        working-directory: red/webview_browser   # Adjust this after you confirm folder structure
        run: flutter pub get

      - name: Build iOS release (no signing)
        working-directory: red/webview_browser
        run: flutter build ios --no-codesign

      - name: Package IPA
        run: |
          cd red/webview_browser/build/ios/iphoneos
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r Runner.ipa Payload
          mv Runner.ipa ../Runner.ipa
          ls -lh ../Runner.ipa

      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v4.6.2
        with:
          name: Runner.ipa
          path: red/webview_browser/build/ios/Runner.ipa
          if-no-files-found: error
          retention-days: 7
          compression-level: 6
          overwrite: true
          include-hidden-files: false
